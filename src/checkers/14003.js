'use strict';

// 14003 가장 긴 증가하는 부분 수열 5 — N 이 100만까지 커진 14002 다.
// 지문은 "정답이 될 수 있는 가장 긴 증가하는 부분 수열을 출력한다" 라고만 적어 자유 선택 문구가
// 없지만, LIS 는 길이만 유일하고 수열은 여럿일 수 있어 BOJ 도 스페셜 저지로 채점한다.
// 기준 풀이는 둘째 줄을 줄바꿈으로 구분해 출력하므로, 문자열 엄격 비교로는 공백으로 구분한
// 올바른 풀이가 전부 오답이 된다.
const { createLisChecker } = require('./lis');

module.exports = { check: createLisChecker(14003) };
