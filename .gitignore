# Runtime / orchestration state
.omx/state/
.omx/logs/
.omx/team/
server-*.log
server-*.err

# Dependencies / build outputs
node_modules/
dist/
build/
coverage/
.judge-tmp/
tmp/
*.out
*.exe
*.o

# Local environment
.env
.DS_Store

# Local OMX runtime metadata
.omx/*.json

# Local OMC / Claude Code runtime metadata (session/orchestration state)
.omc/
.claude/

# Local one-off judge verification reports (generated, not tracked)
bug/

# Special judge handoff package (프라이빗 테스트케이스 6MB 포함, 커밋하면 안 됨)
handoff/
