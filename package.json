{
  "name": "judge04",
  "version": "0.1.0",
  "private": true,
  "description": "Local API-only C++ judge demo backed by data.json.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node scripts/start.js",
    "start:bare": "node src/server.js",
    "check": "node scripts/check.js",
    "test": "npm run check && node --test test/*.test.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED"
}
